{% set name = "pyweed" %}
{% set version = "1.0.6.dev0" %}
{% set sha256 = "db1c789e6017ebd64587fd477caa9c710a7b81c1761458f8b3cb8f7bef12e559" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt
  # pyqt and obspy greatly restrict the available platforms here
  skip: True  # [py2k]
  entry_points:
    - pyweed = pyweed.pyweed_launcher:launch

requirements:
  host:
    - python
    - setuptools
  run:
    - python
    - basemap
    - matplotlib 3.2.*
    - obspy 1.2.*
    - pyqt 5.9.*
    - pyproj


test:
  imports:
    - pyweed
  commands:
    - python -m pyweed.tests

about:
  home: https://iris-edu.github.io/pyweed/
  dev_url: https://github.com/iris-edu/pyweed
  license: LGPL-3.0
  license_file: LICENSE
  summary: "PyWEED: A cross-platform app for retrieving event-based seismic data."

extra:
  recipe-maintainers:
    - adam-iris
